---
description: List sound packs, or switch active pack
argument-hint: "[pack-name]"
---

Run `~/.claude/sounds/switch-pack.sh $ARGUMENTS` via the Bash tool and report the result in one line. No commentary, no fluff — just the output (current pack, available packs, or "Active pack: X" confirmation).
