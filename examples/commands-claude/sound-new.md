---
description: Scaffold a new sound pack folder with a pool.conf template
argument-hint: "<pack-name>"
---

Run `~/.claude/sounds/scripts/new-pack.sh $ARGUMENTS` via the Bash tool. Report the new pack path and remind the user of the three remaining steps (drop wavs, edit pool.conf, switch). Under 8 lines.
