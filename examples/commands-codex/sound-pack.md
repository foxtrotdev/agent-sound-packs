Run the shell command `bash "$HOME/.claude/sounds/scripts/sound.sh" $ARGUMENTS` (subcommands: list, switch <name>, volume <0-100>, mute, unmute, update [name|--all], test, new <name>, add <name>, remote, validate <name>, help) and report the result concisely — just the relevant output (current/active pack, catalog table, or a confirmation). No commentary.
